name: Deploy to Hetzner VPS

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            # Navega para a raiz do projeto no servidor
            cd /home/nuno/sites/ativos.pt
            
            # Vai buscar as últimas alterações
            git pull
            
            # Executa o Docker Compose, especificando o caminho para o ficheiro de configuração
            echo "🐳 A reconstruir e reiniciar os contentores..."
            docker compose -f packages/webapp/docker-compose.yml up --build -d --force-recreate